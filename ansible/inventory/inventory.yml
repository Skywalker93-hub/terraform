all:
 children:
   internal service infrastucture:
     hosts:
       web:
         ansible_host: IP
         ansible_user: ubuntu
         ansible_port: 22
	  ansible_ssh_private_key_file: ~/.ssh/id_rsa
